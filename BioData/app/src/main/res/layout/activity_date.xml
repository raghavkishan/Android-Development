<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/activity_person"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    tools:context="com.example.raghavkishan.biodata.PersonActivity">

    <DatePicker
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:id="@+id/date_picker"
        android:datePickerMode="spinner"/>

    <Button
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="@string/done"
        android:textAllCaps="false"
        android:layout_marginTop="330dp"
        android:layout_marginEnd="55dp"
        android:id="@+id/date_done_button"
        android:layout_alignParentTop="true"
        android:layout_alignEnd="@+id/date_picker" />

    <Button
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="@string/cancel"
        android:textAllCaps="false"
        android:id="@+id/date_cancel_button"
        android:layout_marginStart="44dp"
        android:layout_alignBaseline="@+id/date_done_button"
        android:layout_alignBottom="@+id/date_done_button"
        android:layout_alignParentStart="true" />
</RelativeLayout>